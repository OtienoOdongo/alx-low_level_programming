#include "main.h"

/**
 *reverse_string - reverse string.
 *@s: pointer to string.
 *
 *Return: void.
 */

void reverse_string(char *s)
{
	int i, j, k, temp;

	i = 0;
	while (s[i] != '\0)
	{
		i++;
	}
	k = 0;
	j = i - l;

	while (k < j)
	{
		temp = s[k];
		s[k] = s[j]
		s[j] = temp'
